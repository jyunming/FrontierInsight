% Frontier Insight - fixed 36"x48" 3-column poster template.
% Substitutions (Python string.Template): title, left, middle, right
% (each one is referenced below as $$name in the body — that
% form is what gets replaced by safe_substitute).
% NOTE: every dollar-sign-prefixed variable name in the rest of THIS
% comment is doubled on purpose. string.Template reads the doubled
% form as a literal single dollar, so the comment renders as plain
% prose without being substituted itself. If you let a bare dollar-
% prefixed name appear in this comment, multi-line LLM-generated
% content (which can contain its own \begin/end{column} markers)
% gets injected INTO this comment line; the second and later lines
% are no longer inside a "%" comment and end up as raw LaTeX BEFORE
% \documentclass — pdflatex then errors on \textbf with
% "Undefined control sequence".
\documentclass[final,t]{beamer}
\usepackage[size=custom,width=121.92,height=91.44,scale=1.0]{beamerposter}
\usepackage{graphicx}
\usepackage{multicol}
\usepackage{amsmath, amssymb}
\usepackage{lmodern}
\usepackage{hyperref}

\title{$title}
\author{Frontier Insight}
\institute{Generated by an automated research pipeline}

\begin{document}
\begin{frame}[t]
  \begin{columns}[t]
    \begin{column}{0.32\linewidth}
      $left
    \end{column}
    \begin{column}{0.32\linewidth}
      $middle
    \end{column}
    \begin{column}{0.32\linewidth}
      $right
    \end{column}
  \end{columns}
\end{frame}
\end{document}
