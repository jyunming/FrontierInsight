# Auto-generated by the Frontier Insight VSCode extension.
# You can edit this file and re-run with `@fi /start <path>`.

topic: |
  mammal evolution

title: "mammal-evolution"

provider:
  name: "copilot_cli"

engine:
  framework: "langgraph"
  max_iterations: 2
  review_loop: true
  clarify_mode: "off"
  ideate_reflect: false
  cross_check_per_finding_k: 0
  enable_analyze_reroute: false
  review_panel: []

execution:
  sandbox: "venv"
  timeout_s: 600

knowledge:
  enabled: false
  external_fallback: ["openalex", "arxiv", "crossref"]
  source_routing: "manual"

output:
  kinds: ["paper_md"]
  paper_format: "generic"
  output_dir: "./outputs"
